Use poetry to create and handle virtual envs, with pyenv to manage versions
Use poetry cli to create or add packages, avoid directly editing env files if cli is possible
Use poetry run command to run scripts
Before taking any steps, make sure to create a plan, and discuss it with me, and wait for me to approve the plan
Don't generate documentation files unless prompted
